<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
        crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>

    <style>
        .profile-card {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
        }
        
        .profile-card img {
            width: 100%;
            height: 250px;
            object-fit: cover;
        }
        
        .card-body {
            background-color: #f9f9f9;
            padding: 20px;
        }

        .card-title {
            font-size: 1.75rem;
            font-weight: bold;
            color: #333;
        }

        .card-text {
            font-size: 1.1rem;
            color: #555;
        }

        .card-footer {
            background-color: #f9f9f9;
            text-align: right;
        }

        .btn-edit {
            background-color: #007bff;
            color: white;
            border-radius: 5px;
        }

        .btn-edit:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<%- include('navbar3') %>

<body>
    <div class="container mt-5">
        <h1 class="text-center mb-5" style="font-family: 'Arial', sans-serif;">User Profile</h1>
        <% if (msg) { %>
            <h3><%= msg %></h3>
        <% } %>
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card profile-card">
                    <img src="<%= profile ? profile.photo : null%>" class="card-img-top" alt="Profile Photo">
                    <div class="card-body">
                        <h5 class="card-title"><%= profile ? profile.fullName : null  %></h5>
                        <p class="card-text"><strong>Address:</strong> <%= profile ? profile.address : null %></p>
                        <p class="card-text"><strong>Phone Number:</strong> <%= profile ? profile.phoneNumber : null %></p>
                    </div>
                    <div class="card-footer">
                        <a href="/profile/<%= user.id %>/edit" class="btn btn-edit">Edit Profile</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
